# Scripts

Run these from the repository root:

```bash
bash scripts/smoke.sh
bash scripts/reproduce_main_table.sh
bash scripts/three_method_compare.sh
```

The scripts are intentionally short wrappers around the tested launch scripts
in `llada/`.

For exact ablations or historical server runs, see `llada/run_*.sh`.
